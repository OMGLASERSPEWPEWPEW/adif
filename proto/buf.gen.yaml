version: v2
# C# codegen is handled by Grpc.Tools MSBuild integration in the .csproj.
# Rust codegen is handled by prost-build in tests/proto-rust/build.rs.
# This file is reserved for future language targets that use buf plugins.
plugins: []
